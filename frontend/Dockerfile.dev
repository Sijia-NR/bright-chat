# 开发环境 Dockerfile - 运行 Vite 开发服务器
FROM node:20-alpine

WORKDIR /app

# 安装依赖
COPY package*.json ./
RUN npm ci

# 暴露端口
EXPOSE 5173

# 开发模式：运行 Vite 开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
