# BrightChat AI工作台 - Docker 部署环境变量配置
# 复制此文件为 .env 并修改相应的值

# ============================================
# 应用配置
# ============================================
APP_DEBUG=false
SERVER_WORKERS=4
LOG_LEVEL=INFO

# ============================================
# 端口配置
# ============================================
# 前端端口（容器内部，仅供其他容器访问）
FRONTEND_PORT=8080
# 后端 API 端口（容器内部，仅供其他容器访问）
BACKEND_PORT=18080
# MySQL 数据库端口（主机访问端口）
MYSQL_PORT=13306
# Redis 端口（主机访问端口）
REDIS_PORT=6379
# Nginx HTTP 端口
NGINX_HTTP_PORT=80
# Nginx HTTPS 端口（可选）
NGINX_HTTPS_PORT=443

# ============================================
# MySQL 数据库配置
# ============================================
# ⚠️ 生产环境必须修改默认密码
MYSQL_ROOT_PASSWORD=root_password_change_me
MYSQL_DATABASE=bright_chat
MYSQL_USER=bright_chat
MYSQL_PASSWORD=bright_chat_password

# ============================================
# JWT 认证配置
# ============================================
# ⚠️ 生产环境必须使用强密钥（至少32字符）
JWT_SECRET_KEY=change-this-to-a-strong-secret-key-min-32-chars

# ============================================
# 前端配置
# ============================================
# API 地址（相对于 Nginx）
VITE_API_BASE_URL=/api/v1

# ============================================
# 其他配置
# ============================================
# 时区
TZ=Asia/Shanghai
